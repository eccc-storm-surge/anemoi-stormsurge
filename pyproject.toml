[project]
authors = [{name = "Nils Kristensen", email = ""}]
dependencies = ["anemoi-training>=0.17.0,<0.18", "anemoi-models>=0.19.0,<0.20", "anemoi-graphs>=0.9.7,<0.10", "anemoi-datasets>=0.5.43,<0.6"]
name = "anemoi-stormsurge"
requires-python = ">= 3.11"
version = "0.1.0"

[build-system]
build-backend = "hatchling.build"
requires = ["hatchling"]

[tool.pixi.workspace]
channels = ["conda-forge"]
platforms = ["linux-64"]

[tool.pixi.pypi-dependencies]
anemoi_stormsurge = { path = ".", editable = true }

[tool.pixi.tasks]
